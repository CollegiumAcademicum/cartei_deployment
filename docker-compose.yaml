services:
  migrate:
    image: docker.io/philippbtz/cartei-db:latest
    env_file: .env
    restart: on-failure

  app:
    image: docker.io/philippbtz/cartei-web:latest
    env_file: .env
    volumes:
      - ./certs:/app/certs:ro,z
    networks:
      - www
    depends_on:
      - migrate
    restart: unless-stopped

networks:
  www:
    external: true
